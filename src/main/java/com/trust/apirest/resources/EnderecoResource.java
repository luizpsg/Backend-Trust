package com.trust.apirest.resources;

/*
@RestController
@RequestMapping(value = "/enderecos")
public class EnderecoResource {

  @Autowired
  private EnderecoService service;

  // Endpoint para reatribuir um Endereco existente a um novo cliente
  @PutMapping("/{enderecoId}/cliente/{clienteId}")
  public ResponseEntity<Void> reassigEndToClient(@PathVariable Long enderecoId, @PathVariable Long clienteId) {
      service.reassignEndToClient(enderecoId, clienteId);
      return ResponseEntity.ok().build();  // Retorna uma resposta 200 OK se a operação for bem-sucedida
  }

}
 */