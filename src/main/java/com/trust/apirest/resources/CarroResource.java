package com.trust.apirest.resources;

/*
@RestController
@RequestMapping(value = "/carros")
public class CarroResource {

  @Autowired
  private CarroService service;


  // Endpoint para reatribuir um carro existente a um novo cliente
  @PutMapping("/{carroId}/cliente/{clienteId}")
  public ResponseEntity<Void> reassignCarToClient(@PathVariable Long carroId, @PathVariable Long clienteId) {
      service.reassignCarToClient(carroId, clienteId);
      return ResponseEntity.ok().build();  // Retorna uma resposta 200 OK se a operação for bem-sucedida
  }
 
}
*/